import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: "100%",
        backgroundColor: "#4F63AC",
        paddingVertical: 20,
        paddingHorizontal: 8,
        borderRadius: 8,
    },
    title: {
        color: "white",
        textAlign: "center",
        fontSize: 16,
        fontWeight: "bold",
    },
});